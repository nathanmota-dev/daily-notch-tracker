{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the DailyNotch main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
