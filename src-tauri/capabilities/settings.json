{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Capability for the DailyNotch settings window",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
